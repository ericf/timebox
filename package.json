{
  "name": "timebox",
  "version": "1.0.0",
  "private": true,
  "author": "Eric Ferraiuolo <edf@ericf.me>",
  "license": "MIT",
  "homepage": "https://timebox.now.sh",
  "devDependencies": {
    "dotenv-safe": "^2.3.2",
    "node-fetch": "^1.6.3",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "firebase": "^3.2.1",
    "list": "^0.8.2",
    "marked": "^0.3.6",
    "react": "^15.2.1",
    "react-native-web": "0.0.47",
    "react-router": "^4.0.0-alpha.3"
  },
  "scripts": {
    "dev": "node scripts/check-env && react-scripts start",
    "build": "node scripts/check-env && react-scripts build",
    "start": "list build --single --cache 31556926",
    "now": "now -e REACT_APP_API_KEY=@timebox_prod_api_key -e REACT_APP_AUTH_DOMAIN=@timebox_prod_auth_domain -e REACT_APP_DATABASE_URL=@timebox_prod_database_url",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
